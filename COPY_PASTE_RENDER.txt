# 📋 COPY-PASTE COMMANDS FOR RENDER DEPLOYMENT

## STEP 1: Push to GitHub

### You already have the repo! Just push the changes:

```powershell
git add .
git commit -m "Fix Render deployment configuration"
git push origin main
```

If you get authentication error, authenticate with GitHub first.

---

## STEP 2: Deploy on Render

1. Go to: https://dashboard.render.com
2. Click "New +" → "Static Site"
3. Connect GitHub and select repository: "reddingtonglobal/ReddingtonGroupINC"
4. Use these settings:

**IMPORTANT - Leave Root Directory EMPTY or use `.`**

**Build Command:**
```
npm install && npm run build
```

**Publish Directory:**
```
build
```

⚠️ DO NOT set a custom root directory like "frontend" or "src"!

---

## STEP 3: Update Your Website Later

```powershell
git add .
git commit -m "Updated website"
git push origin main
```

---

## DNS Records for Your Domain

### A Record:
```
Type: A
Name: @
Value: 216.24.57.1
```

### CNAME Record:
```
Type: CNAME
Name: www
Value: your-site-name.onrender.com
```

Replace `your-site-name` with the actual name Render gives you!

---

## Troubleshooting Commands

### Check if git is initialized:
```powershell
git status
```

### Test build locally:
```powershell
npm run build
```

### Start local server:
```powershell
npm start
```

### Check DNS propagation:
Visit: https://dnschecker.org

---

**That's it! Simple and easy!** 🚀
